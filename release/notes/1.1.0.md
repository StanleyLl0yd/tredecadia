# Tredecadia 1.1.0

> DRAFT — NOT AUTHORIZED FOR PUBLICATION

Stable v1.1 is planned as a compatible localization-only minor release derived from published `v1.1.0-rc.1`.

Planned stable promotion:

- preserve the complete canonical v1 calendar identity from `v1.0.0`;
- preserve the existing stable `ru-Cyrl`, `ja-Kana`, and `ko-Hang` month profiles unchanged;
- promote the six explicitly accepted reviewed RC profiles — `ka-Geor`, `hy-Armn`, `ar-Arab`, `hi-Deva`, `bn-Beng`, and `fa-Arab` — to `stable` maturity;
- introduce no weekday aliases;
- change only release version/status metadata and the maturity of explicitly accepted profiles.

All six profile decisions are complete and accepted. Publication remains blocked solely by the independent RC observation/approval gate: the observation must complete after its minimum interval with no compatibility-critical finding and no open report before the atomic stable transition can be authorized.
