# Tredecadia 1.0.0

> **DRAFT — NOT AUTHORIZED FOR PUBLICATION.** This file is stable-release groundwork only. Publication remains blocked by `release/stable-plan.json` until the RC2 observation/review gate is explicitly completed and the stable-release decision is approved.

Tredecadia 1.0.0 is intended to stabilize the v1 compatibility surface as published in `v1.0.0-rc.2`. The earlier `v1.0.0-rc.1` remains an immutable historical predecessor; RC2 is the direct stable baseline after the pre-stable weekday/schema compatibility correction.

The stable release must not change the RC2 calendar identity: 13 × 28 regular structure, Tredecadia Era/epoch, `EQ`/`ED` semantics, civil conversion and leap rule, canonical date grammar, canonical month order/names/Short-6/Short-4 forms, canonical weekday order/IDs/names (`W1 Mene · W2 Noko · W3 Kese · W4 Zoyo · W5 Sote · W6 Yemo · W7 Toze`), pronunciation identity, conservative Short-4 recognition policy, or reviewed month-localization mappings.

Expected stable-only transitions, once explicitly authorized, are limited to release maturity and metadata:

- version identifiers `1.0.0-rc.2` → `1.0.0`;
- registry `status` values `draft` → `stable`;
- explicitly accepted localization profiles `reviewed` → `stable`;
- stable citation/changelog/release metadata;
- deterministic stable archive/checksum publication;
- DOI/archive handoff after immutable stable publication.

Any compatibility-critical correction discovered during RC2 observation or final stable review requires renewed RC work instead of being hidden in this stable transition.
