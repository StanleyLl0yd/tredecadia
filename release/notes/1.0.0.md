# Tredecadia 1.0.0

> **DRAFT — NOT AUTHORIZED FOR PUBLICATION.** This file is stable-release groundwork only. Publication remains blocked by `release/stable-plan.json` until the RC observation/review gate and explicit localization-profile decisions are complete.

Tredecadia 1.0.0 is intended to stabilize the v1 compatibility surface already exercised by `v1.0.0-rc.1`.

The stable release must not change the RC calendar identity: 13 × 28 regular structure, Tredecadia Era/epoch, `EQ`/`ED` semantics, civil conversion and leap rule, canonical date grammar, canonical month order/names/Short-6/Short-4 forms, pronunciation identity, or conservative Short-4 recognition policy.

Expected stable-only transitions, once explicitly authorized, are limited to release maturity and metadata:

- version identifiers `1.0.0-rc.1` → `1.0.0`;
- accepted registry `status` values `draft` → `stable`;
- explicitly accepted localization profiles `reviewed` → `stable`;
- stable citation/changelog/release metadata;
- deterministic stable archive/checksum publication;
- DOI/archive handoff after immutable stable publication.

Any compatibility-critical correction discovered during RC review requires renewed RC work instead of being hidden in this stable transition.
